# services/ai

AI assistant (RAG over platform knowledge). **Not started** — Phase 1.

The parked C# prototype at `C:\ProjectRepos\Personal\ai-ops-assistant` will be
**rewritten in Python** and folded in here.
